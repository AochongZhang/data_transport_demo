<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhangaochong.data_transport_demo.dao.UserDao">
    <update id="update">
        update ${tableName} set update_date = current_timestamp;
    </update>

    <select id="selectTest" resultType="com.zhangaochong.data_transport_demo.entity.User">
        select * from ${tableName}
    </select>
</mapper>